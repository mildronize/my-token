# 3: What's the console's theme color?

Type: wayfinder:grilling
Status: resolved
Blocked by: None (can start immediately)

## Question

The source ticket raises theme color as an explicit open question.

## Answer

Amber/gold accent (`#F59E0B`-ish) on a dark neutral background. Reads as
"cost/usage" without red's alarm connotation (this isn't an incident console)
or blue's generic-dashboard-default feel; also visually distinct from
typ-fleet UI's own console so the two aren't confused when both are open.
Not cross-checked against typ-fleet UI's actual palette — no documented
color found in fleet memory — but มายด์ confirmed amber/gold directly.
